# Runtime dependencies for the API (what the Dockerfile installs). Pinned to the versions
# verified in .venv on 2026-09-15. The root requirements.txt is the full dev/CI freeze.
fastapi==0.139.0
uvicorn[standard]==0.49.0
websockets==16.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
sqlmodel==0.0.39
networkx==3.4.2
numpy==2.5.0
ollama==0.6.2
google-generativeai==0.8.3
anthropic==0.42.0
# Only imported when OPENROUTER_API_KEY is set (api/llm/router.py); not in the local venv,
# so it carries a compatible range rather than an unverified exact pin.
openai>=1.50.0,<3
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.4.0
