---
title: Session Handoff
date: 2026-06-18T22:32:00
project: CivilizationOS
repo: https://github.com/syzayd/CivilizationOS
---

# Session Handoff - 2026-06-18

## Summary
Started CivilizationOS from an empty directory and shipped **Phases 0 and 1** - a
working, $0-to-run multi-agent society sim. The backend simulates ~10 autonomous
citizens living daily routines with episodic memory + local-LLM conversations; the
frontend renders an animated isometric city with an agent inspector. Both phases are
committed cleanly and pushed to GitHub. 28 tests passing.

## Project concept (north star)
A hybrid **AGORA × PANTHEON**: autonomous citizens live in a city, and (Phase 2) five
institutions are each run by a 5-specialist council that *debates* using a novel RAG
(**TCMF** - Temporal-Causal Memory Fusion). User injects crises and watches society react.
Must satisfy 4 pillars: multi-agent, novel RAG, fine-tuned model + MLOps, full-stack
(Claude API + frontend). Hard constraint: runs for **under $20** (user is a student).
Full plan: `C:\Users\Asus\.claude\plans\lock-in-i-d-build-iterative-meteor.md`.

## Work Completed

**Phase 0 - Foundation** (commit `907d61f`)
- `api/llm/router.py` - 3-tier cost-aware LLM router (Tier 0 Ollama / Tier 1 Gemini /
  Tier 2 Claude) with `PREMIUM_MODE` downgrade rules + hard USD spend cap. The core
  cost-control mechanism.
- `api/config.py` - pydantic-settings with safe $0 defaults (runs without `.env`).
- `api/main.py` - FastAPI: `/health`, `/llm/ping`, `/ws`.
- `api/memory/vectorstore.py` - numpy cosine vector store (no native deps).
- `web/` - React + Vite + TS + PixiJS + Zustand scaffold.

**Phase 1 - AGORA living city** (commit `6092038`)
- `api/sim/world.py` - grid city, locations (homes/workplaces/commons/institutions),
  world clock + day phases (night/morning/work/evening).
- `api/sim/engine.py` - deterministic seedable tick loop; movement, co-location
  detection, throttled conversations, end-of-day reflection. LLM calls are
  **event-driven + non-blocking** (background tasks) so the tick loop never stalls.
- `api/agents/citizen.py` + `personas.py` - 10 citizens w/ routines, relationships.
- `api/memory/stream.py` - episodic memory with `relevance × recency × importance`
  retrieval + reflection. (The AGORA half of the eventual TCMF RAG.)
- `web/src/city/CityStage.tsx` + `iso.ts` - PixiJS isometric renderer (animated
  citizens, speech bubbles, day/night tint, click-to-select).
- `web/src/panels/Inspector.tsx` + `EventFeed.tsx` - agent mind inspector + city feed.
- `web/src/ErrorBoundary.tsx` - catches render errors.
- Verified live in-browser: citizens move by routine, hold coherent in-character
  LLM conversations (e.g. nurse Hugo ↔ doctor Ava at the clinic), form memories +
  reflections - all at $0. Screenshots in `docs/screenshots/`.

## Dead Ends - Do Not Retry

### Python / dependency setup
| Approach Tried | Why It Failed | Fix |
|---|---|---|
| Python 3.14 (pre-installed) | ML stack lags; risky | Installed Python 3.12.10 via winget; venv uses it |
| `chromadb` for vectors | `chroma-hnswlib` has no 3.12 Windows wheel; needs MSVC build tools; aborted whole install | Dropped Chroma → `api/memory/vectorstore.py` (numpy). Plenty fast at this scale |
| `httpx==0.28.1` | Conflicts: `ollama` requires `httpx<0.28` | Pinned `httpx==0.27.2` |

### Frontend render (blank page debugging - ~45 min)
| Approach Tried | Why It Failed | Fix |
|---|---|---|
| Writing errors to `#root` in an index.html catcher | Corrupted React's container → cascading `removeChildFromContainer` masking the real error | Use a separate sink / ErrorBoundary instead |
| MCP `browser_execute_script` to read page `window.*` globals | Runs in an **isolated world** - shares DOM but not page JS globals (store invisible) | Verified via DOM queries + temporary default selection instead |
| MCP `browser_click(x,y)` on the Pixi canvas | Synthetic click didn't reach Pixi's federated event system (reported clicking canvas center) | Couldn't verify click→select via tool; verified inspector by temporarily defaulting `selectedId` |

**Real root cause of the blank page**: zustand footgun in `EventFeed.tsx` -
`useWorld((s) => s.world?.events ?? [])` created a **new `[]` every render**, so
zustand's identity check looped forever ("Maximum update depth exceeded"). Fix: move
the `?? []` **outside** the selector. Lesson: never return a fresh object/array from a
zustand selector.

### React StrictMode + Pixi
- StrictMode double-invokes effects in dev, racing the imperative Pixi canvas
  lifecycle. Dropped StrictMode in `web/src/main.tsx` (prod never double-invokes).

## Key Decisions
- **3-tier brain router, local-first**: routine agent life on Ollama (Qwen2.5 3B),
  Claude reserved for Phase-2 council debates. Keeps the whole sim at ~$0. Trade-off:
  3B dialogue is simpler than Claude's - acceptable for ambient citizen chatter.
- **Event-driven, non-blocking LLM**: conversation/reflection generation runs as
  background asyncio tasks; movement stays smooth, costs stay bounded.
- **numpy vector store over Chroma**: avoids Windows native-build pain, and gives full
  control over TCMF scoring in Phase 2.
- **In-memory sim (no DB yet)**: SQLite/persistence deferred; in-memory is fine for now.
- **GitHub: public repo `syzayd/CivilizationOS`**, one clean commit per phase
  (squashed the noisy auto-commits).

## Pending Issues
- [ ] Auto-commit hook noise - MED - global `Write|Edit` PostToolUse hook in
  `C:\Users\Asus\.claude\settings.json` (line 61) commits per file. **User is applying
  a fix**: a `case` guard so paths under `projects/new` stage-only (commit elsewhere).
  Verify with: edit a file here → `git log --oneline -1` shows no new `auto:` commit.
- [ ] Citizen name labels overlap when several share a location - LOW - Phase 5 polish.
- [ ] `.env` not creatable by assistant (security hook blocks it) - user must
  `cp .env.example .env` to add Gemini/Claude keys - LOW.
- [ ] No persistence: sim resets on backend restart - LOW (by design for now).

## Next Steps (Phase 2 - PANTHEON councils + TCMF RAG)
1. `api/memory/causal_graph.py` - NetworkX temporal causal graph (event → decision →
   outcome → downstream event).
2. `api/memory/tcmf.py` - fuse episodic memory streams with causal-graph traversal
   (the signature novel RAG). Reuse `vectorstore.py` + `MemoryStream`.
3. `api/agents/council.py` - 5-specialist debate (Historian/Strategist/Skeptic/
   Predictor/Synthesizer); Claude (Tier 2) in premium mode, local/Gemini in free mode.
4. `web/src/panels/CouncilChamber.tsx` - live debate transcript UI.
5. Add unit tests for TCMF retrieval (seeded causal precedent ordering).

## How to run
```bash
# backend (from repo root)
.venv/Scripts/python -m uvicorn api.main:app --reload --port 8000
# faster demo clock: set env TICK_SECONDS=0.1 before launching
# frontend
cd web && npm run dev      # http://localhost:5173
# tests
.venv/Scripts/python -m pytest
```

## Context for Future Sessions
- Windows + PowerShell primary; Bash tool also available. **Background commands do NOT
  inherit the repo cwd** - use absolute paths (e.g. `/c/Users/Asus/projects/new/...`).
- `PYTHONIOENCODING=utf-8` needed when printing LLM output to the console (cp1252 errors).
- Ollama installed at `C:/Users/Asus/AppData/Local/Programs/Ollama`; models pulled:
  `qwen2.5:3b-instruct`, `nomic-embed-text`. Server runs as a background service.
- Subscriptions (Claude Pro / Gemini / Perplexity) are chat UIs - **not** usable as
  programmatic APIs. Free programmatic options: local Ollama + Google AI Studio free tier.
- Model IDs (verify via claude-api skill): `claude-haiku-4-5` (council members),
  `claude-sonnet-4-6` (synthesis). Pricing in `router.py` is an estimate for the cap.
- Laptop GPU: NVIDIA 4–6 GB VRAM (drives local model choice).
```
