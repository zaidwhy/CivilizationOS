# Handoff - 2026-09-18 16:13 IST - TCMF paper: faculty finalization + N19 audit triage

Scope: `research/tcmf_paper/` (this repo, public) and the private draft repo `zaidwhy/tcmf-paper`,
checked out in place at `research/tcmf_paper/paper/` (gitignored here; push from that folder goes
to the private repo). Run commands live in this repo's CLAUDE.md.

## What changed this session

1. Zaid asked to finalize/review/audit/polish the TCMF paper into a PDF for his department
   faculty. Fresh full read of `main.tex` + `references.bib`, rebuilt from scratch, checked for
   what a departmental committee (not an ML-workshop reviewer) would flag - stray markers,
   hardcoded numbering, citation completeness, visual layout via direct page rasterization. No
   new defects found; the prior session's N08-N17 work had already addressed nearly everything.
   Copied the clean build to `Downloads/TCMF-Paper-Faculty-Review.pdf`.
2. Asked directly "is this ready for publication" - answered honestly rather than reassuring:
   ready for faculty, not fully ready for a research venue, since no qualified human has ever
   read it end to end hunting for reasons to reject it. Zaid chose to send the as-is PDF to 3-4
   faculty and decide next steps from their reviews.
3. Zaid relayed a full AI-generated external audit (docx) plus his own independent critique of
   it, asking to refine the paper using both without following either blindly. Read the audit
   directly (not just Zaid's summary), checked every specific claim in both against the live
   manuscript. Nearly everything flagged as "must fix" was already fixed in prior work; caught
   the audit contradicting itself in one place and asserting a false "unaddressed anomaly"
   claim against F9b in another.
4. **N19**: the one real, previously-undone gap - the BM25 scaffolding-artifact claim was
   argued in prose but never actually verified by rerunning it. Checked the generator code
   first to confirm the rerun was cheap and isolated to BM25 alone, then built and ran it.
   Pure regime: recall@5 0.00 -> 0.06, confirming the artifact (still far below TCMF's 1.00).
   Mixed regime: causal@5 bit-for-bit unchanged - never an artifact there, confirmed by reading
   the actual generator templates, not inferred. A runtime sanity check in the script caught
   and recorded a second, unrelated, out-of-scope text-overlap property of the mixed regime.
5. Deliberately not chased, matching Zaid's own judgment: LLM-induced causal graphs, a full
   HippoRAG 2 reimplementation, open-ended LLM-as-judge decision scoring - real future-paper
   directions, not confirmed gaps in this one.

## Current state

- Both repos committed, pushed, in sync with origin. Working trees clean.
- `tcmfbench` test suite: 4 new tests (`test_n19_bm25_descaffold.py`), all passing; full suite
  green throughout (verified before and after the change).
- `main.pdf` builds clean: 29 pages, 0 undefined refs/citations, same 4 pre-existing overfull
  hboxes never exceeded, verified by rasterizing and directly reading both changed pages.
- `Downloads/TCMF-Paper-Faculty-Review.pdf` is current with this session's build - it is the
  copy Zaid is sending to faculty.

## Decisions made, and why

- **The audit's own explanation ("root-cause episodic score approaches zero") was never
  imported into the paper**, because it's the exact wrong explanation N15/F3 already disproved
  in a prior session (mean episodic score 0.96, not near zero). Confirmed the paper never said
  this rather than assuming it was safe.
- **BM25's mixed-regime causal@5 failure is reported as genuine, not scaffolding.** The mixed
  generator's memory-text templates (`mixed.py`: `"witness 0"`, `"ancestor 0"`, etc.) never
  embedded a topic id in the first place - confirmed by reading the source, not by the
  unchanged number alone (a silently-broken strip regex could produce the same "no change"
  result by accident, which is exactly why the script has a runtime sanity assertion).
- **The private repo's remote is `zaidwhy/tcmf-paper`, not `syzayd/tcmf-paper`** - some older
  memory/log entries in this codebase still say `syzayd`; that's stale, confirmed by `git
  remote -v` this session. Worth fixing wherever it recurs, not treating as authoritative.

## Known issues

- Same 4 pre-existing overfull hboxes as always (4-18pt, cosmetic, unrelated to this session).
- The draft has never been compiled against a venue style file (still generic `article`).

## Exact next steps

1. **Wait for the 3-4 faculty reviews Zaid is about to send the PDF for.** When feedback comes
   back, triage each comment against the actual manuscript the same way this session triaged
   the external audit - check what's real, what's already handled, what's a misreading -
   rather than applying anything blindly.
2. The paid external hostile-reviewer pass (REVIEW.md section 5/6) remains the other open item
   before submission-readiness - still explicitly Zaid's call.
3. If faculty feedback conflicts across reviewers, flag that explicitly rather than picking one
   silently - it's useful signal about which comments are load-bearing vs. taste.

## Do not repeat

- Do not re-run the BM25 descaffolding experiment with default `GenConfig()`/`MixedConfig()` -
  it must use `n_distractors=20, n_noise=55` (the realistic pool) to match the published
  Table tab:more-baselines numbers exactly. The default small-pool config gives different,
  non-comparable numbers (this was caught and fixed mid-session, not shipped wrong).
- Do not treat a relayed external audit's numbers as current without checking them - this
  session found one internally inconsistent both in itself (table says "Oracle (Ceiling)",
  its own prose two paragraphs later says "oracle control") and against the live manuscript,
  most likely from reading a version of the draft that predates a fix already landed.
