# Handoff - 2026-09-17 00:00 IST

## What was done

1. **Found and fixed a real correctness bug in `docs/tcmf.md`.** While writing a system-
   design case study for personal-llm and recall (matching this repo's existing
   `ARCHITECTURE.md`), discovered that `docs/tcmf.md` - the technical writeup the
   published GitHub-profile case study directly links to as proof of "both versions, the
   failure and the fix" - actually described the OLD, disproven multiplicative fusion
   formula as current, with no mention it was ever wrong. Confirmed the real code
   (`api/memory/tcmf.py`) uses the normalized-additive formula, matching
   `PROFILE-FACTS.yaml`. Rewrote the doc against the real code and
   `research/tcmf_paper/FINDINGS.md`'s actual numbers: `tcmf_mult` recall@5 = 0.02
   (against causal_only's 1.00), shipped `TCMFRetriever` recall@5 = 0.76 / recall@10 =
   1.00 with the root cause at rank 1 (not a flat 1.00 - that's only the isolated
   additive-operator study, not what's shipped).
2. **M4 - structured per-call LLM observability.** `api/llm/router.py`'s `complete()`
   now wraps every call and emits one JSON log line on the `civos.llm.calls` logger:
   `call_id`, `tier_requested`, `tier_used`, `downgraded`, `model`, `latency_ms`,
   `cost_usd`, `input_tokens`/`output_tokens`, `ok`, `error`. Emitted on success AND on
   a raised exception, so a failed call is visible instead of just missing.
3. `ARCHITECTURE.md`'s Observability section rewritten to match.

## Current state

- `pytest api/tests -q` green (ran the full suite, all dots, no failures - the exact
  count wasn't captured cleanly from the terminal output this session, but zero F/E
  characters across the run confirms no regressions).
- Manually verified the new structured logging both succeeding and failing (mocked
  Ollama client in both cases) - correct JSON in both paths, exception still propagates
  after logging.
- `git status -sb` clean, no ahead/behind.

## Next steps

- None outstanding for M4. `zaid-os/dashboard/SCORECARD.md`'s MLOps row still has one
  gap left: an actual dashboard/visualization over these structured log lines, not just
  the raw JSON in the log stream.
- `docs/tcmf.md` and the case study are now consistent; no further action needed unless
  the underlying formula changes again, in which case both need updating together this
  time.

## How to resume

Run commands live in `CLAUDE.md`.
