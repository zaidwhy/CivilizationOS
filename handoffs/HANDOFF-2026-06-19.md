---
title: Session Handoff
date: 2026-06-19
project: CivilizationOS
repo: https://github.com/syzayd/CivilizationOS
---

# Session Handoff - 2026-06-19

## Summary

Resumed from Phase 1 (which was fully working). Shipped **Phases 2, 3, and 4**,
then did a full cross-phase refinement pass. 48 tests passing, TypeScript clean,
backend imports clean. All committed and pushed to GitHub.

---

## What Was Built This Session

### Phase 2 - PANTHEON Councils + TCMF RAG
- `api/memory/causal_graph.py` - NetworkX DiGraph. Nodes = societal events. Edges = causal
  links (A caused B). BFS predecessor traversal with depth limit.
- `api/memory/tcmf.py` - **TCMF retriever**: fuses citizen episodic memory scores with causal
  graph depth boosts. Formula: `tcmf_score = episodic_score × (1 + λ × causal_boost)`.
  Memories near causal ancestors of the current crisis rank higher.
- `api/agents/council.py` - 5-role council debate streaming as AsyncGenerator:
  Historian (📜) → Strategist (⚔️) → Skeptic (🔍) → Predictor (🔮) → Synthesizer (⚖️).
  Synthesizer issues `VERDICT:` directive. Tier.PREMIUM (Claude) for Synthesizer, FREE for others.
- `api/sim/crisis.py` - Crisis registry + debate transcript store.
- `web/src/panels/CouncilChamber.tsx` - live debate UI, crisis injection form.
- `POST /crisis`, `GET /debates/{id}`, `GET /crises` endpoints.

### Phase 3 - Crises + Society Dynamics
- `api/sim/events.py` - 5 crisis templates:
  - 🦠 **Pandemic** → closes clinic, high fear, triggers Healthcare + Government
  - 🌵 **Drought** → closes market, triggers Economy + Government
  - 💻 **Cyberattack** → closes City Hall, triggers Government + Media + Police
  - 🗳️ **Election** → contested results, triggers Government + Media
  - 🔫 **Crime Wave** → closes park, triggers Police + Media
- Citizens gain `fear` (0–1) on crisis injection, decays at 0.006/tick (~2 days to clear).
- Citizens with fear > 0.7 shelter at home regardless of time of day.
- Location closure reroutes citizens to alternative destinations.
- Secondary institutions activate in parallel background debates.
- `web/src/panels/RelationshipGraph.tsx` - canvas social graph (fear heatmap, co-location edges).
- `GET /events/templates` endpoint for frontend presets.

### Phase 4 - Fine-Tuning + MLOps
- `ml/dataset/generate.py` - generates 300-sample instruction-tuning dataset
  (5 roles × 60 crisis scenarios). Run: `python -m ml.dataset.generate --n 60`
- `ml/dataset/council_voices.jsonl` - 300 pre-generated training samples (committed).
- `ml/train_lora.ipynb` - full Unsloth LoRA training notebook for free Colab T4:
  loads Qwen2.5 3B at 4-bit, trains with SFTTrainer, runs persona-consistency eval
  gate, exports GGUF (Q4_K_M) if gate passes. MLflow tracking built in.
- `ml/evals/persona_eval.py` - standalone eval harness: persona-consistency
  (role-specific keyword patterns) + debate-coherence (Jaccard word overlap).
  Logs to MLflow. Promotion gate: overall_pass_rate ≥ 70%.
- `api/config.py` - `OLLAMA_COUNCIL_MODEL` setting (empty = use base model).
- `api/llm/router.py` - `local_model` override parameter on `complete()`.
- Council deliberation automatically uses fine-tuned model when configured.

### Cross-Phase Refinements
- **Engine**: fixed double `world.snapshot()` call; added `active_crises` +
  `closed_locations` to world snapshot; `agent_detail` now includes `fear` +
  `active_crisis`; memory count expanded to 15.
- **Conversations**: now mention active crisis + relationship warmth.
- **Reflections**: now mention active crisis + citizen fear level.
- **Relationships**: slow decay (0.0003/tick) + sociability-weighted growth.
- **Fear**: decay rate 0.002 → 0.006 (realistic 2-day clearance).
- **Council**: max_tokens 120 → 180, cleaner role system prompts with `VERDICT:` mandate.
- **Router**: `local_model` override for Phase 4 fine-tuned council model.
- **EventFeed**: icons + color coding per event kind, active crisis badges.
- **Inspector**: fear bar with percentage + active crisis label.
- **CityStage**: citizens tint from base color → red as fear rises (`citizenColorWithFear`).
- **Topbar**: active crisis pills in red.
- **Sidebar CSS**: fixed from `grid-template-rows: 1fr auto` to `flex-direction: column`
  so all 4 panels render correctly.

---

## File Map (complete)

```
api/
  config.py                  # settings incl. OLLAMA_COUNCIL_MODEL
  main.py                    # FastAPI: /health, /agent/{id}, /crisis, /debates, /crises, /events/templates
  requirements.txt           # added mlflow==2.20.2
  agents/
    citizen.py               # fear state, decay_fear, decay_relationships, closed-location routing
    council.py               # 5-role debate, fine-tuned model integration
    personas.py              # 10 seed citizens
  llm/
    router.py                # 3-tier router + local_model override
  memory/
    causal_graph.py          # NetworkX causal event graph
    stream.py                # episodic memory (relevance×recency×importance)
    tcmf.py                  # TCMF retriever (the novel RAG)
    vectorstore.py           # numpy cosine store
  sim/
    crisis.py                # Crisis dataclass + CrisisRegistry
    engine.py                # main tick loop, crisis injection, debate orchestration
    events.py                # 5 crisis templates
    world.py                 # grid, locations, day phases
  tests/
    test_crisis.py           # 10 tests (fear, closure, templates)
    test_engine.py           # 5 tests
    test_memory_stream.py    # 7 tests
    test_router.py           # 9 tests
    test_tcmf.py             # 10 tests
    test_vectorstore.py      # 7 tests  [TOTAL: 48, all passing]

ml/
  dataset/
    generate.py              # synthetic council-voice dataset generator
    council_voices.jsonl     # 300 training samples (pre-generated)
  evals/
    persona_eval.py          # persona-consistency + coherence eval + MLflow logging
  train_lora.ipynb           # Unsloth LoRA notebook (Colab T4 ready)
  mlruns/                    # local MLflow tracking store (empty until training run)

web/src/
  App.tsx                    # topbar crisis pills, all panels mounted
  index.css                  # sidebar flex layout, fear-bar styles
  city/
    CityStage.tsx            # PixiJS renderer, fear-tinted citizen dots
    iso.ts                   # citizenColorWithFear()
  panels/
    CouncilChamber.tsx       # crisis presets, debate transcript
    EventFeed.tsx            # icons, colors, active-crisis badges
    Inspector.tsx            # fear bar, active_crisis label
    RelationshipGraph.tsx    # canvas social graph
  ws/
    store.ts                 # WorldMessage (active_crises, closed_locations), AgentDetail (fear)
```

---

## How to Run

```powershell
# Backend (from repo root)
$env:PYTHONIOENCODING="utf-8"
.venv\Scripts\python -m uvicorn api.main:app --reload --port 8000

# Frontend
cd web; npm run dev   # http://localhost:5173

# Tests
.venv\Scripts\python -m pytest

# Generate more training data
.venv\Scripts\python -m ml.dataset.generate --n 100 --out ml/dataset/council_voices.jsonl

# Run persona eval (requires Ollama running)
.venv\Scripts\python -m ml.evals.persona_eval --model qwen2.5:3b-instruct --n 5
```

---

## Inject a Crisis

```bash
# Via UI: CouncilChamber panel → pick preset → Inject Crisis
# Via curl:
curl -X POST http://localhost:8000/crisis \
  -H "Content-Type: application/json" \
  -d '{"text":"Fever outbreak at Mercy Clinic","institution_id":"inst_health","template_key":"pandemic"}'
```

---

## Phase 4 Training Workflow (when ready)

1. Open `ml/train_lora.ipynb` in Google Colab (free T4).
2. Upload `ml/dataset/council_voices.jsonl` to Colab.
3. Run all cells - training ~20 min, exports GGUF if persona gate passes.
4. Download `.gguf` + `mlruns/` folder.
5. On laptop: `ollama create council-voice -f Modelfile` (see notebook cell 6).
6. Add `OLLAMA_COUNCIL_MODEL=council-voice` to `.env`.
7. Restart backend - councils now use the fine-tuned voice.

---

## Next Steps - Phase 5 (Polish + Demo)

1. Visual polish: tooltips, onboarding tour, location labels on hover.
2. "Rewind story" - timeline slider to replay the causal graph.
3. Persistent SQLite storage (engine state survives restart).
4. Record 2-minute demo video: inject 3 crises → councils debate → city reacts.
5. Deploy frontend to Vercel; document local-run instructions.
6. Architecture diagram + TCMF explainer writeup.

---

## Persistent Issues / Notes

- Auto-commit hook in `C:\Users\Asus\.claude\settings.json` commits every file write.
  Each session: after coding, do `git reset --soft HEAD~N && git commit -m "Phase X: ..."`.
- `PYTHONIOENCODING=utf-8` required in PowerShell before running backend (cp1252 issues).
- Ollama runs as Windows background service - no need to start manually.
- `.env` cannot be created by assistant (security hook). User must `cp .env.example .env`.
- `ml/train_lora.ipynb` requires Colab - cannot run locally (no GPU for LoRA).
- `mlflow` added to requirements - run `pip install -r api/requirements.txt` to get it.
